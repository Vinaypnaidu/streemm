# k8s/config/secrets.yaml

---
apiVersion: v1
kind: Secret
metadata:
  name: streemm-secrets
  namespace: streemm
type: Opaque
stringData:
  # Database credentials
  POSTGRES_USER: streemm
  POSTGRES_PASSWORD: streemm
  DATABASE_URL: postgresql+psycopg://streemm:streemm@postgres:5432/streemm_dev
  
  # Redis cache
  REDIS_URL: redis://redis:6379/0
  
  # Session signing secret
  SESSION_SECRET: changeme-please-use-a-real-secret-in-production
  
  # S3/MinIO credentials
  S3_ACCESS_KEY: minioadmin
  S3_SECRET_KEY: minioadmin
  
  # OpenSearch credentials
  OPENSEARCH_USERNAME: opensearchadmin
  OPENSEARCH_PASSWORD: Vinay@Open5e@rch
